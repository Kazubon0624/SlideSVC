# SlideSVC インストール & セットアップ手順

他のMacユーザーが SlideSVC を使用するための手順です。

## 1. アプリケーションの配置
1. 配布された `SlideSVC.zip` をダブルクリックして解凍します。
2. 展開された `SlideSVC.app` を、Macの **「アプリケーション」** フォルダ（`/Applications`）に移動（ドラッグ＆ドロップ）します。

---

## 2. セキュリティ警告（ゲートキーパー）の解除
本アプリは開発用証明書でビルドされているため、別のMacで初めて開く際に「開発元を検証できないため開けません」という警告ダイアログが表示されます。以下のいずれかの方法で解除してください。

### 方法A: 右クリックから開く（推奨）
1. 「アプリケーション」フォルダ内の `SlideSVC.app` を **右クリック（または Control キーを押しながらクリック）** します。
2. メニューから **「開く」** を選択します。
3. 警告ダイアログが表示されますが、今回は「開く」ボタンが選択可能ですので、**「開く」** をクリックします。
4. 一度この操作を行えば、以降は通常通り起動できるようになります。

### 方法B: ターミナルコマンドでの解除
ターミナルを開き、以下のコマンドを実行します。これにより拡張属性（Quarantineフラグ）が削除されます。
```bash
xattr -cr /Applications/SlideSVC.app
```

---

## 3. Quick Look 拡張機能の有効化
`SlideSVC.app` を「アプリケーション」フォルダに配置すると、macOSが自動的に Quick Look 拡張機能（`.svs`, `.ndpi`, `.scn` 等のプレビュー機能）を認識します。

もしFinderでスペースキーを押してもプレビューが表示されない場合は、以下の手順をお試しください。

1. **Quick Look キャッシュのリセット**:
   ターミナルを開き、以下のコマンドを実行してキャッシュを再構築します。
   ```bash
   qlmanage -r
   qlmanage -r cache
   ```
2. **アプリの直接起動**:
   一度 `SlideSVC.app` をダブルクリックして直接起動させてから（バックグラウンドの常駐エージェントとして起動します）、再度Finderでプレビューをお試しください。
3. **拡張機能設定の確認**:
   - macOSの「システム設定」 ＞ 「プライバシーとセキュリティ」 ＞ 「拡張機能」（または「システム設定」 ＞ 「キーボード」 ＞ 「キーボードショートカット」 ＞ 「サービス/拡張機能」など、OSバージョンにより異なります）を開きます。
   - **「Quick Look」** カテゴリを選択し、`SlideQLPreview` および `SlideQLThumbnail` にチェックが入っていることを確認します。
